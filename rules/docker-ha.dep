DPATH       := $($(DOCKER_HA)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-ha.mk rules/docker-ha.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_HA)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_HA)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_HA)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_HA),$(DOCKER_HA_DBG)))
